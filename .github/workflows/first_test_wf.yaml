name: fist ws

on:
  push:

jobs:
  first-job:
    run-on: ubuntu-latest
    steps:
        - name: check java version
          run: java -version
        - name: check maven version
          run: mvn -version