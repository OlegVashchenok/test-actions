name: greeting action use

on:
  workflow_dispatch:

jobs:
 job_1:
    runs-on: ubuntu-latest
    steps:
        - name: Greeting
          uses: ./.github/actions/greeting
          with:
            name: 'Olha'
        - name: print message
          run: echo ${{ steps.greting.outputs.message }}

